# Traitors AI - Frontend

React + Tailwind CSS frontend for visualizing game replays.

## Setup

```bash
npm install
npm start
```

The app will open at http://localhost:3000

## Backend Required

Make sure the FastAPI backend is running:

```bash
cd ../backend
pip install -r requirements.txt
python app.py
```

Backend runs at http://localhost:8000
